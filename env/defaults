GS_ENV=production

COMPOSE_PROJECT_NAME=project
APP_PREFIX=project
SERVICE_NAME=pgadmin

PGADMIN_HTTP_PORT=80

PGADMIN_VOLUME_NAME=pgadmin_data_v4
PGADMIN_DEFAULT_EMAIL=admin@bmd.studio
PGADMIN_USER_DIR=admin
PGADMIN_DEFAULT_PASSWORD=password

POSTGRES_HOST_NAME=postgresql
POSTGRES_PORT=5432
POSTGRES_DEFAULT_DATABASE_NAME=postgres
POSTGRES_SUPER_USER_ROLE_NAME=postgres
POSTGRES_SUPER_USER_SECRET=password

PROXY_ROUTER_ENABLE=true
PROXY_ROUTER_DOMAIN={domain:.+}

DOCKER_BASE_IMAGE=dpage/pgadmin4:4.27
DOCKER_RESTART_POLICY=unless-stopped
DOCKER_LOGGING_DRIVER=json-file
DOCKER_LOGGING_MAX_FILE=10
DOCKER_LOGGING_MAX_SIZE=20m
DOCKER_VOLUME_BIND_FLAG=cached
DOCKER_BACKEND_NETWORK_NAME=backend
DOCKER_FRONTEND_NETWORK_NAME=frontend